# === Rust / Cargo ===
# Compiled binary output
**/target/

# Cargo package files
**/*.rs.bk

# Cargo.lock is usually committed for binaries but ignored for libraries
# Uncomment the next line if this is a library project
# Cargo.lock

# === IDE / Editor ===
# VSCodium / VSCode settings and workspace files
.vscode/
*.code-workspace

# === OS / Environment ===
# Linux system files
*~
*.swp
.DS_Store       # in case files are moved/copied from macOS
.cache/